# Changelog

## 0.7.0 - 2026-08-27

- 增加可配置的平滑视觉反馈，刷新时进度条会过渡到最新额度，刷新图标旋转并伴随轻量状态呼吸效果。
- 额度达到 80%/95% 时自动切换注意/错误色，进度条增加柔和高光和悬停边框，提升小尺寸窗口的可读性。
- 设置新增“平滑动效与状态反馈”，默认开启；关闭后立即回到静态绘制，适合低性能设备或减少动效偏好。
- 重置时间在后台以低频刷新保持准确，不增加网络请求；动效使用单一 UI 定时器并在托盘隐藏时停止。

## 0.6.0 - 2026-08-27

- 增加可操作诊断中心，展示 OAuth、网络、额度、缓存、历史和开机自启检查项。
- 每项诊断提供通过/注意/失败状态和固定的下一步建议。
- 支持在诊断中心重新检查、复制安全摘要，支持 `Ctrl+R` 快捷刷新。
- 诊断窗口支持高 DPI、主题、可调整大小，并保持不出现在 `Alt+Tab`。
- 增加独立错误色和动态检查项布局，失败状态更易识别。

## 0.5.0 - 2026-08-27

- 增加 7/30/90 天历史保留周期，读取旧文件时同步清理超期记录。
- 增加开机后的首次查询延迟（0/5/15/30 秒），便于代理和 Codex CLI 完成初始化。
- 增加可选的启动时更新提示，默认关闭且不会自动下载或替换程序。
- 刷新与更新检查共享进行中任务，避免并行请求和重复提示。
- 诊断摘要新增历史保留、启动延迟和启动更新检查状态。

## 0.4.1 - 2026-08-27

- 为可执行文件补充正式产品、文件和信息版本元数据。
- 诊断页、文件属性和更新检查统一显示 `0.4.1`。

## 0.4.0 - 2026-08-27

- 增加统一额度 Provider 模型，支持官方动态窗口并保留 Free、Go、Plus、Pro、Team、Business、Enterprise、Edu 等已知计划名。
- 增加本地成功缓存和 30 天百分比历史；网络/OAuth 失败时保留旧数据并明确标记缓存，右键可清理本地数据。
- 增加双击/右键详情窗口，展示全部窗口、重置日期、最近成功时间和趋势图；详情与设置窗口支持 DPI 缩放且不进入 `Alt+Tab`。
- 增加跟随系统、深色、浅色和石墨主题，主状态栏启用 PerMonitorV2 并按当前显示器定位。
- 增加 GitHub Release 手动更新检查和 SHA-256 校验辅助，不自动覆盖运行中的文件。
- 诊断摘要增加版本、DPI、缓存/历史状态；P1 smoke 覆盖持久化、隐私、窗口标志和校验逻辑。

## 0.3.0 - 2026-08-27

- 增加通知区域托盘：点击 `×` 隐藏而不退出，双击托盘图标恢复，托盘菜单支持刷新、设置、诊断和退出。
- 增加设置窗口：可选择 1/5/10/15/30/60 分钟刷新周期、实色/两档透明度、记住窗口位置和开机启动。
- 增加可选额度阈值通知，默认关闭，只在使用率跨越阈值时提醒一次，避免重复打扰。
- 增加白名单诊断服务，固定输出安全元数据，不包含 Token、账户 ID、代理地址、文件路径或完整响应。
- 状态栏与设置窗口均不进入 `Alt+Tab` 切换列表，继续保持单实例和轻量 WinForms 运行方式。
- GitHub Actions、源码编译命令和中文教程改为自动编译根目录全部 C# 源文件。

## 0.2.0 - 2026-08-27

- 默认启用当前 Windows 用户的开机自启，可从状态栏右键菜单关闭或重新开启。
- 增加单实例保护，避免开机自启和手动启动产生重复窗口。
- 增加立即刷新、运行诊断、复制脱敏诊断信息和打开项目主页入口。
- 诊断摘要不包含 Token、账户 ID、代理地址或完整接口响应。
- README、中文教程、贡献指南和 Bug 模板同步补充使用说明。

## 0.1.3 - 2026-08-26

- 统一使用跨计划的 ChatGPT / Codex 表述。
- 保留官方额度窗口、下一次重置日期时间和可选 HTTP/HTTPS 代理支持。
